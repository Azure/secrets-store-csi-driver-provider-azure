trigger: none

pr:
  branches:
    include:
    - master
    - release-*
  paths:
    exclude:
      - docs/*
      - website/*
      - README.md

pool: staging-pool-amd64-mariner-2

jobs:
  - template: templates/e2e-test-azure.yaml
    parameters:
      osTypes:
      - "linux"
      - "windows_docker"
      - "windows_containerd"
  # this will ensure any changes to provider works on arc extension too.
  # xref: https://github.com/Azure/secrets-store-csi-driver-provider-azure/issues/1382 for tracking
  # - template: templates/arc/e2e-extension-test.yaml
