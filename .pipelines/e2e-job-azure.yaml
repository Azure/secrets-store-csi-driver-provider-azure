trigger: none

pr:
  branches:
    include:
    - master
    - release-*
  paths:
    exclude:
      - docs/*

pool: staging-pool

jobs:
  - template: templates/e2e-test-azure.yaml
    parameters:
      clusterTypes:
      - "aks"
      # - "aks-engine"
      osTypes:
      - "linux"
      # - "windows"
