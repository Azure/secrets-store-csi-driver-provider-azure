steps:
  - bash: |
      az role assignment delete --ids ${ROLE_ASSIGNMENT_IDS} > /dev/null
      az keyvault delete-policy --name $(KEYVAULT_NAME) --object-id "${ASSIGNEE_OBJECT_ID}" > /dev/null
    condition: always()
    displayName: Cleanup role assignments