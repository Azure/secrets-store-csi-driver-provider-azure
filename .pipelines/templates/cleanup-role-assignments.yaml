steps:
  - bash: |
      az role assignment delete --ids ${ROLE_ASSIGNMENT_IDS} > /dev/null
      az keyvault delete-policy -n ${KEYVAULT_NAME} --object-id ${USER_MSI_PRINCIPAL_ID} > /dev/null
    condition: always()
    displayName: Cleanup role assignments
