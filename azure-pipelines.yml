trigger: none

pr:
  branches:
    include:
      - master
      - release-*
  paths:
    exclude:
      - docs/*
      - website/*

pool: staging-pool

jobs:
  - template: .pipelines/templates/arc/arc-e2e-test-kind.yaml
  - template: .pipelines/templates/arc/arc-e2e-extension-test.yaml
  # - template: .pipelines/templates/unit-test.yaml
  # - template: .pipelines/templates/e2e-test-kind.yaml
